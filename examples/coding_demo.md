# 實戰操作：撰寫程式 (Writing Code)

## 情境
我們需要在一個 Python 專案中，實作一個簡單的網頁標題抓取功能 (`scraper.py`)，並且為其加上測試。

---

## 傳統做法 (Traditional Workflow)
1.  **思考**：我需要 `requests` 和 `BeautifulSoup`。
2.  **搜尋/詢問**：在 ChatGPT 輸入「寫一個 Python 函數用 requests 和 bs4 抓取 title」。
3.  **複製貼上**：將生成的程式碼複製。
4.  **切換視窗**：打開 VS Code，建立 `scraper.py`，貼上程式碼。
5.  **執行測試**：手動執行 `python scraper.py`，發現沒有安裝套件。
6.  **除錯**：執行 `pip install requests beautifulsoup4`。
7.  **再次執行**：發現程式碼有語法錯誤或邏輯漏洞，複製錯誤訊息回 ChatGPT...

🔴 **缺點**：頻繁切換視窗、手工操作多、容易打斷心流。

---

## Antigravity 做法 (Agentic Workflow)

### 1. 啟動對話 (Chat Interface)
在 IDE 右側的 **Chat 面板**（或使用快捷鍵 `Cmd+L` 喚起），輸入以下高層次指令：

> "在 `src/scraper.py` 實作一個抓取網頁標題的功能（使用 requests 和 bs4），並在 `tests/test_scraper.py` 加上相應的 pytest 測試案例。請確保處理連線錯誤的情況。"

### 2. 審閱與執行 (Review & Execute)
Agent 接收指令後會顯示擬定的計畫。
1.  **確認計畫**：查看 Agent 打算建立哪些檔案。
2.  **一鍵執行**：點擊 **「Run」** 或 **「Proceed」** 按鈕。

### 3. Agent 自動化執行 (Automation)
Agent 會自動進行以下步驟，你只需在螢幕上看著它操作：

1.  **環境檢查**：檢查 `requirements.txt` 是否有相關套件，若無則建議加入或直接安裝。
2.  **建立檔案**：自動建立 `src/scraper.py` 和 `tests/test_scraper.py`。
3.  **撰寫程式碼**：
    ```python
    # src/scraper.py
    import requests
    from bs4 import BeautifulSoup

    def get_page_title(url):
        try:
            response = requests.get(url, timeout=10)
            response.raise_for_status()
            soup = BeautifulSoup(response.text, 'html.parser')
            return soup.title.string.strip() if soup.title else None
        except Exception as e:
            print(f"Error fetching {url}: {e}")
            return None
    ```
4.  **驗證**：Agent 主動執行 `pytest`。
    - *即便失敗*：如果測試失敗，Agent 會讀取錯誤訊息，自動修正程式碼，直到測試通過。

### 4. 你的工作：審閱 (Review)
當任務完成後，IDE 會顯示 **Diff View**（差異檢視模式）。
- **檢查變更**：左側是原檔案（若有），右側是新內容。
- **接受變更**：如果滿意，點擊 **「Accept」** 或 **「Commit」** 保存變更。

🟢 **優點**：
- **零切換**：你停留在 IDE 中。
- **自動閉迴路**：Agent 自己寫、自己測、自己修。
- **專注架構**：你關注的是「功能介面」與「錯誤處理策略」，而非 `import` 語法細節。
